package queries

import (
	// Import this so we don't have to use qm.Limit etc.

	. "github.com/volatiletech/sqlboiler/v4/queries/qm"
)

/*func GetUser(db *sql.DB, uuid string) models.User {
    user, err := models.Users(

    )
}*/
